import { StyleSheet } from "react-native"

export default StyleSheet.create({
    pendingTasks:{
        backgroundColor:'#fff',
        width: '48%',
        height:90,
        borderRadius: 10,
        justifyContent:'center',
        alignItems:'center',
        padding:10
    }
})